image: ruby:2.3.1

pipelines:
  default:
    - step:
        script:
          - ruby --version
          - bundler --version
          - bundle install
          - RAILS_ENV=test bundle exec rails db:schema:load
          - bundle exec rspec
          # - bin/cucmber
