= simple_form_for [:admin, category], url: url do |f|
  = f.input :title, label: 'Название'
  = f.input :url, label: 'URL', html: {novalidate: true}
  = f.button :submit,  category.new_record? ? 'Создать' : 'Обновить'